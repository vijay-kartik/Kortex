<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Kortex" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Share-intake dialog: fully transparent window so the Compose scrim + card float
         over whatever app the user shared from. The scrim is drawn in Compose. -->
    <style name="Theme.Kortex.Share" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
    </style>
</resources>
